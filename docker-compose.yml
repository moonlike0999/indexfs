version: '3.9'

services:
  caddy:
    pull_policy: build
    build: .
    dns:
      - 1.1.1.1
      - 8.8.8.8
    ports:
      - "80:80"
      - "443:443"
      - "443:443/udp"
    volumes:
      - ${FS_DIR}:/fs
      - ${APPDATA_DIR}:/data
      - ${CONFIG_DIR}:/config
    restart: always
